---
apiVersion: v1
kind: Service
metadata:
  name: myservice
  labels:
    author: chaitanya
spec:
  type: ClusterIP
  ports:
    - targetPort: 80
      port: 80
  selector:
    author: chaitanya
    type: proxy
